name: Deploy mdBook site to Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      MDBOOK_VERSION: "0.5.2"
      MDBOOK_MERMAID_VERSION: "0.17.0"
      MDBOOK_UTILS_VERSION: "0.1.3"
    steps:
      - uses: actions/checkout@v4

      # 1. 安装 Rust 工具链
      - name: 安装 Rust 工具链
        run: |
          curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh -s -- -y
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      # 2. 使用专用 Action 安装 mdBook（带缓存）
      - name: 安装 mdBook
        uses: taiki-e/cache-cargo-install-action@v1
        with:
          tool: mdbook@${{ env.MDBOOK_VERSION }}

      # 3. 使用专用 Action 安装 mdbook-mermaid（带缓存）
      - name: 安装 mdbook-mermaid
        uses: taiki-e/cache-cargo-install-action@v1
        with:
          tool: mdbook-mermaid@${{ env.MDBOOK_MERMAID_VERSION }}

      # 4. 使用专用 Action 安装 mdbook-utils（带缓存）
      - name: 安装 mdbook-utils
        uses: taiki-e/cache-cargo-install-action@v1
        with:
          tool: mdbook-utils@${{ env.MDBOOK_UTILS_VERSION }}

      # 5. 构建与部署
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Build with mdBook
        run: mdbook build

      - name: 生成 sitemap
        run: mdbook-utils sitemap -b https://vincentme.github.io/zig-tutorial

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
